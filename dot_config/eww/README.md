# Eww Configuration
## Required Initial Manual Steps
- Complete [sway configuration](../sway/README.md) to specify $ewwoutput sway variable
- Copy `sys.yuck.example` to `sys.yuck` and modify its content as required
