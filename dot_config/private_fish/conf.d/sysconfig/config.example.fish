# INFO: This is just an example/template file.
# Copy this file to `./config.fish` to make sure fish runs as expected

set -l SYSCFG_PATH ~/.config/fish/conf.d/sysconfig/

source "$SYSCFG_PATH/vars.fish"
