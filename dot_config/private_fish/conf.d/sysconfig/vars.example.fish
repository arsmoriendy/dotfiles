# INFO: This is just an example/template file.
# Each variable listed here is required to be specified.
# Modify the variables and rename this file to `vars.fish` to make sure fish runs as expected

# distro variables
set -g DISTRO_ICON ""
set -g PACK_MAN "sudo pacman" # distro package manager
